/**
 * 取り込みを何件まで同時に走らせるか。メール経由とアップロード経由で共通。
 *
 * 1リクエストにつき候補1件、という形はどちらの経路でも変えない。
 * ここで増やすのは同時に飛ばすリクエストの本数だけなので、
 * 1回の関数実行の長さは変わらない（＝実行時間の上限には影響しない）。
 *
 * 上げすぎない理由は実行時間ではなく、外部APIの同時実行の受け入れ量:
 * 読み取り1件につき Anthropic へ1本、Gmail へ1〜2本の呼び出しが出る。
 * 断られた場合も請求書の行は作られて画面に残る（黙って消えはしない）が、
 * 手入力の手間が増えるので、失敗が出るようなら下げること。
 */
export const IMPORT_CONCURRENCY = 3;
