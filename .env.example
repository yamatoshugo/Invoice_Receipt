# --- データベース (Neon Postgres) ---
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# --- 認証 (Auth.js v5 + Google) ---
# openssl rand -base64 32 などで生成する
AUTH_SECRET=""
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
# このシステムを使えるGoogleアカウントをカンマ区切りで列挙する。
# ここに無いアカウントはログインできない。
ALLOWED_EMAILS="you@example.com"

# --- ファイル保管 (Vercel Blob) ---
BLOB_READ_WRITE_TOKEN=""

# --- 請求書の読み取り (Anthropic) ---
ANTHROPIC_API_KEY=""

# --- ここから下はローカル動作確認専用。本番(Vercel)には設定しないこと ---
#
# デプロイ前に手元で操作を確認するための設定。
# Google OAuth / Vercel Blob / Anthropic の登録なしで一通り触れるようにする。
# 手順は README.md の「ローカルで動かす」を参照。

# メールアドレスを入れるだけでログインできる簡易ログインを有効にする。
# NODE_ENV=production では、この値を立てても有効にならない。
# AUTH_DEV_LOGIN="1"

# PDFの保管先を Vercel Blob ではなくローカルの .uploads/ にする。
# STORAGE="local"

# PDFを読まずにダミーの読み取り結果を返す。
# 本番環境で指定すると起動時にエラーになる（偽の口座情報が振込CSVに載るのを防ぐため）。
# EXTRACTOR="stub"
